# Минимальные зависимости для локального запуска (без CrewAI)
# pip install -r requirements-minimal.txt
# Экономит ~500 MB по сравнению с полным requirements.txt

aiogram>=3.0
fastapi>=0.139.2,<0.140
uvicorn[standard]>=0.27,<1
jinja2
sqlalchemy
psycopg2-binary
alembic
pyyaml
python-dotenv
httpx
aiohttp-socks>=0.8.4
